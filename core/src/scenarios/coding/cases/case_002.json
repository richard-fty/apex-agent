{
  "id": "vite_empty_state_001",
  "title": "Add an empty state",
  "tier": "C1",
  "template": "vite-react",
  "input": "Add a friendly empty state for when the rendered list has no items.",
  "prompt": "Add a friendly empty state for when the rendered list has no items.",
  "allowed_paths": ["src/**", "package.json"],
  "playwright_spec": "cases/case_002/tests/empty-state.spec.mjs",
  "golden_patch": "cases/case_002/golden/patch.diff",
  "max_steps": 30,
  "manifest": {
    "install": ["pnpm", "install", "--frozen-lockfile", "--prefer-offline"],
    "build": ["pnpm", "build"],
    "test": ["pnpm", "exec", "playwright", "test", "cases/case_002/tests/empty-state.spec.mjs", "--reporter=json"],
    "budget": { "max_steps": 30, "max_cost_usd": 0.5, "max_wall_sec": 300 },
    "limits": { "memory_mb": 1024, "cpus": 1.0, "max_stdout_kb": 2048 }
  }
}
