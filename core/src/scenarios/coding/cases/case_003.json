{
  "id": "vite_filter_list_001",
  "title": "Add filter input to a list",
  "tier": "C2",
  "template": "vite-react",
  "input": "Add a search input above the list that filters items as the user types.",
  "prompt": "Add a search input above the list that filters items as the user types.",
  "allowed_paths": ["src/**", "package.json"],
  "playwright_spec": "cases/case_003/tests/filter-list.spec.mjs",
  "golden_patch": "cases/case_003/golden/patch.diff",
  "max_steps": 30,
  "manifest": {
    "install": ["pnpm", "install", "--frozen-lockfile", "--prefer-offline"],
    "build": ["pnpm", "build"],
    "test": ["pnpm", "exec", "playwright", "test", "cases/case_003/tests/filter-list.spec.mjs", "--reporter=json"],
    "budget": { "max_steps": 30, "max_cost_usd": 0.5, "max_wall_sec": 300 },
    "limits": { "memory_mb": 1024, "cpus": 1.0, "max_stdout_kb": 2048 }
  }
}
