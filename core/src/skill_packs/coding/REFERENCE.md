# Coding Reference

MVP coding evals use Vite, React, TypeScript, and Playwright.

Default gates:
- `pnpm install --frozen-lockfile --prefer-offline`
- `pnpm build`
- `pnpm exec playwright test --reporter=json`

